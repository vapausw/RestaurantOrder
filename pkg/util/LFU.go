package util

// 实现一个LFU缓存淘汰算法，控制各个缓存的大小，防止内存泄漏
// 有一个问题，redis在某一个键的时间过期了之后，导致这个键的值被删除，但是这个键在LFU中的频率并没有被删除
// 这个问题可以通过在redis中设置一个键的过期时间，然后在LFU中设置一个键的过期时间，当这个键的过期时间到了之后，就可以删除这个键，同步双键的过期时间
// LFU中如何自动删除过期的键呢？可以通过一个定时器，每隔一段时间检查一次LFU中的键的过期时间，然后删除过期的键
